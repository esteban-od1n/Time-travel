<VirtualHost [::1]>
	Define DIR '/srv/www/apache/Time-travel/public'

	ServerAdmin renko@dev
	ServerName Time-travel.dev
	DocumentRoot ${DIR}
	<Directory ${DIR}>
		Require all granted
		RewriteEngine On
		RewriteRule ^(.*)$ index.php
	</Directory>
</VirtualHost>
